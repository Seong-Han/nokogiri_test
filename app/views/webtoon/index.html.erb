<div class="container" style="text-align:center">
    <h1> 당신이 좋아하는 웹툰만 남겨보세요~</h1>
    
    <% @content.each do |y| %>
    <div class="container">
       <h5 class="mt-3 mb-0"> <%= y.content %></h5>
       <img src="<%= y.url %>"><br>
        <a href="/webtoon/destroy/<%= y.id %>"> <button type="button" class="btn btn-secondary py-0">삭제</button> </a>
        <a href="/webtoon/edit/<%= y.id %>"> <button type="button" class="btn btn-secondary py-0">수정</button> </a>
        <br>
        </div>
    <% end %>
    
</div>